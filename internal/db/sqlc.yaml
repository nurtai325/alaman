version: "2"
sql:
  - engine: "postgresql"
    queries: "queries"
    schema: "migrations"
    database:
      uri: "postgres://alaman:alaman@localhost:5432/alaman"
    gen:
      go:
        package: "repository"
        out: "repository"
        sql_package: "pgx/v5"
