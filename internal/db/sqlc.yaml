version: "2"
sql:
  - engine: "postgresql"
    queries: "query.sql"
    schema: "schema.sql"
    database:
      uri: "postgres://alaman:alaman@localhost:5432/alaman"
    gen:
      go:
        package: "repository"
        out: "repository"
        sql_package: "pgx/v5"
